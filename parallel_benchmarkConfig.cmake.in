# parallel_benchmark/parallel_benchmarkConfig.cmake.in
# This file is generated during installation and helps find_package(parallel_benchmark) locate the library.

# Provide the installed targets
include("${CMAKE_CURRENT_LIST_DIR}/parallel_benchmark_targets.cmake")

# Set the public include directories for consumers of parallel_benchmark
set(parallel_benchmark_INCLUDE_DIRS "${CMAKE_INSTALL_PREFIX}/include")

# Define variables that indicate the library was found
set(parallel_benchmark_FOUND TRUE)
